# traefik.env.sample
#
# Copy this file to the deploy user’s home directory and rename it:
#   scp traefik.env.sample deploy@server:~/traefik.env
#
# Then edit ~/traefik.env on the server and fill in the real values.
#
# ⚠️ IMPORTANT
# - Do NOT commit the real traefik.env file (it contains secrets).
# - All Traefik tooling will ONLY read from ~/traefik.env.
# - If ~/traefik.env is missing or incomplete, scripts will exit with an error.

########################################
# Cloudflare / Let’s Encrypt settings  #
########################################

# CF_API_TOKEN
# Cloudflare token with:
#   - Zone.DNS:Edit
#   - Zone.Zone:Read
# for the zone that contains your site/domain.
CF_API_TOKEN=CHANGE_ME_CLOUDFLARE_DNS_EDIT_TOKEN

# EMAIL
# Email used for:
#   - Let’s Encrypt registration
#   - Expiry notices
EMAIL=you@example.com

# USE_STAGING
# Set to "true" while testing to avoid Let’s Encrypt rate limits.
# When you’re happy everything works, set to "false" for real certificates.
USE_STAGING=true


########################################
# Webhook / GitHub integration        #
########################################

# WEBHOOK_SECRET
# Shared secret used to validate GitHub webhook payloads.
# - Must match the "Secret" you configure on the GitHub Webhook.
# - Change this to a long random string before using in production.
WEBHOOK_SECRET=ChangeThisSecretNow

# HOOKS_HOST
# The hostname you configured in DNS (Cloudflare) for the webhook service.
# Traefik will use this host for routing the webhook HTTP requests.
# Example: hooks.example.com
HOOKS_HOST=hooks.your-domain.com


########################################
# Core Traefik Tooling                 #
########################################

# TRAEFIK_REPO_URL
# URL to the Traefik-Deployment repository.
TRAEFIK_REPO_URL=https://github.com/joshphillipssr/Traefik-Deployment.git

# TRAEFIK_DIR
# Install location for Traefik on the server.
TRAEFIK_DIR=/opt/traefik

# SITES_DIR
# Base directory where all managed site containers will live.
SITES_DIR=/opt/sites

# DEPLOY_USER
# Linux user account used for all deployment operations.
DEPLOY_USER=deploy